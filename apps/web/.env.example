# ---------------------------------------------------------------------------
# Variables d'environnement — apps/web (Next.js)
# Copier ce fichier en `.env.local` et renseigner les valeurs réelles.
# `.env.local` est ignoré par git (cf. .gitignore) — ne JAMAIS le committer.
#
# Sur Vercel : déclarer ces deux variables dans
#   Project Settings -> Environment Variables (Production + Preview).
# ---------------------------------------------------------------------------

# Projet Supabase — valeurs PUBLIQUES, sûres côté client (préfixe NEXT_PUBLIC_).
# À récupérer dans : Supabase Dashboard -> Project Settings -> API
NEXT_PUBLIC_SUPABASE_URL=https://pjtmmzxcitbcwbbgtpdj.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=
