# Expo + Metro + pnpm compatibility
# Hoist all dependencies to a flat node_modules layout (npm-style) so Metro
# can resolve peer dependencies like @expo/metro-runtime correctly.
# Reference: https://docs.expo.dev/guides/monorepos/
node-linker=hoisted
